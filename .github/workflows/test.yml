name: github-actions-sandbox

on:
  push:
    branches:
      - develop

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Echo
        run: echo Hello World!
      - name: Directory List
        run: ls
